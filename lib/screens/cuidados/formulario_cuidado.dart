import 'package:flutter/material.dart';

class FormularioCuidadoScreen extends StatelessWidget {
  const FormularioCuidadoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cadastrar Cuidado'),
      ),
      body: const Center(
        child: Text('Formulário de Cuidados - Em desenvolvimento'),
      ),
    );
  }
}